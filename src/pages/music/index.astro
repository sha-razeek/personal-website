---
import Layout from '../../layouts/Layout.astro';
import { musicArticles } from '../../data/music';

const musicOnly = musicArticles.filter(a => a.genre !== 'Tech');
---

<Layout title="Music" description="Music writing, playlists, and the Transistor radio player">
  <div class="modernist-grid">
    <section class="grid-section hero-modernist-page">
      <div class="hero-content">
        <span class="hero-eyebrow">The Soundscape</span>
        <h1 class="hero-title">
          <span class="gradient-text">Music</span>
        </h1>
        <p class="hero-description">
          Writing about music, discovering sounds, and tuning in to global frequencies.
        </p>
      </div>
    </section>

    <div class="music-content">
      <!-- Transistor Radio promo -->
      <a href="/music/transistor" class="feature-block-main transistor-feature">
        <div class="feature-img-container">
          <img src="/images/radio.png" alt="Transistor Radio" class="feature-img" />
          <div class="feature-label-overlay">
            <span class="feature-eyebrow">Live Now</span>
            <h3>Transistor Radio</h3>
          </div>
        </div>
      </a>

      <div class="jet-line">
        <span>
          <svg width="24" height="24" viewBox="0 0 164 153" fill="none" xmlns="http://www.w3.org/2000/svg" style="display: block;"><path d="M99.1721 13.564C99.3179 13.5731 99.4635 13.5837 99.6091 13.5958C102.251 13.8165 102.96 15.5127 104.211 17.5868C105.815 20.2447 107.383 23.1732 109.508 25.4607C112.233 28.4162 115.633 30.6685 119.417 32.0254C122.672 33.1866 128.989 34.488 126.517 39.4104C125.627 41.1835 124.601 41.5477 122.788 42.1171C114.185 40.6196 110.737 37.8718 103.963 32.7557C104.165 45.1096 103.876 57.7554 103.969 70.163C104.03 78.142 104.345 87.649 103.296 95.4482C102.787 99.2327 100.609 104.138 98.1743 107.103C92.1085 114.491 86.7581 117.132 77.5628 118.259C69.7081 118.647 62.826 116.707 56.8529 111.446C51.225 106.552 47.7703 99.6238 47.2481 92.184C46.7593 84.4211 48.981 77.0913 54.1697 71.2232C59.1521 65.6068 66.164 62.2032 73.659 61.763C81.6607 61.2856 89.0358 63.9075 95.0335 69.2261C95.2888 57.7363 94.9645 45.9583 95.086 34.4412C95.1362 29.0507 94.9268 23.6458 95.1882 18.261C95.3421 15.0902 96.0907 14.1279 99.1721 13.564Z" fill="currentColor"/><path d="M73.5962 70.5726C84.331 69.5289 93.8802 77.3832 94.9274 88.1176C95.9745 98.8521 88.1234 108.404 77.3893 109.454C66.6503 110.506 57.0932 102.65 56.0455 91.9107C54.9978 81.1713 62.8565 71.6168 73.5962 70.5726Z" fill="currentColor"/></svg>
        </span>
      </div>

      <!-- Music Articles -->
      <section>
        <h2 class="serif-label">Music Writing</h2>
        <div class="blog-modern-grid stagger">
          {musicOnly.map((article) => (
            <a href={`/blog/${article.slug}`} class="blog-item">
              <div 
                class="blog-visual" 
                style={`background: ${article.heroColor}; ${article.heroImage ? `background-image: url('${article.heroImage}'); background-size: cover; background-position: top center;` : ''}`}
              >
              </div>
              <div class="blog-info">
                <div class="blog-meta">
                  <span class="blog-date">{article.date}</span>
                  <span class="blog-tag">{article.genre}</span>
                </div>
                <h3>{article.title}</h3>
                <p>{article.excerpt}</p>
                <span class="blog-link">Read Article →</span>
              </div>
            </a>
          ))}
        </div>
      </section>
    </div>
  </div>
</Layout>

<style>
  .modernist-grid {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    padding-bottom: 4rem;
  }

  .grid-section {
    width: 100%;
  }

  .hero-modernist-page {
    padding: 2rem 0 3rem;
  }

  @media (max-width: 768px) {
    .hero-modernist-page {
      padding: 1rem 0 1.5rem;
    }
  }

  .hero-eyebrow {
    display: block;
    font-family: var(--font-mono);
    text-transform: uppercase;
    letter-spacing: 0.15em;
    font-size: 0.8rem;
    color: var(--accent-primary);
    margin-bottom: 1rem;
  }

  .hero-title {
    font-family: var(--font-serif);
    font-size: clamp(3.5rem, 12vw, 10rem);
    line-height: 0.85;
    font-weight: 900;
    color: var(--color-brown);
    text-transform: uppercase;
    margin-bottom: 2rem;
  }

  .gradient-text {
    background: linear-gradient(
      135deg, 
      var(--color-brown) 0%, 
      var(--accent-primary) 25%, 
      var(--accent-secondary) 50%, 
      var(--accent-primary) 75%, 
      var(--color-brown) 100%
    );
    background-size: 400% auto;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    animation: shimmeryText 12s linear infinite;
    display: inline-block;
  }

  @keyframes shimmeryText {
    0% { background-position: 0% 50%; }
    100% { background-position: 400% 50%; }
  }

  .hero-description {
    font-size: 1.1rem;
    line-height: 1.5;
    color: var(--text-secondary);
    max-width: 600px;
  }

  .music-content { max-width: 1000px; }

  /* Feature Block (Transistor) */
  .transistor-feature {
    height: 280px;
    margin-bottom: 3rem;
  }

  .feature-block-main {
    background: var(--color-dark);
    border-radius: var(--radius-sm);
    overflow: hidden;
    position: relative;
    display: block;
    transition: transform var(--transition-medium);
    border: 1px solid var(--border-color);
  }

  .feature-block-main:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
  }

  .feature-img-container {
    width: 100%;
    height: 100%;
    position: relative;
  }

  .feature-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    opacity: 0.8;
    transition: opacity var(--transition-medium);
  }

  .feature-block-main:hover .feature-img {
    opacity: 1;
  }

  .feature-label-overlay {
    position: absolute;
    bottom: 2rem;
    left: 2rem;
    color: white;
    text-shadow: 0 2px 10px rgba(0,0,0,0.3);
  }

  .feature-eyebrow {
    font-family: var(--font-mono);
    font-size: 0.85rem;
    text-transform: uppercase;
    display: block;
    margin-bottom: 0.5rem;
    color: var(--color-offwhite);
    opacity: 0.9;
    letter-spacing: 0.05em;
  }

  .feature-label-overlay h3 {
    font-family: var(--font-serif);
    font-size: 2.5rem;
    text-transform: uppercase;
    line-height: 1;
  }

  .serif-label {
    font-family: var(--font-serif);
    font-size: 1.8rem;
    text-transform: uppercase;
    color: var(--color-brown);
    margin-bottom: 2rem;
    font-weight: 700;
  }

  .blog-modern-grid {
    display: flex;
    flex-direction: column;
    gap: 3rem;
  }

  .blog-item {
    display: grid;
    grid-template-columns: 240px 1fr;
    gap: 3rem;
    text-decoration: none;
    color: inherit;
    transition: transform var(--transition-medium);
    align-items: center;
  }

  .blog-item:hover {
    transform: translateX(8px);
  }

  .blog-visual {
    width: 100%;
    aspect-ratio: 3/4; /* Tall rectangle */
    border-radius: var(--radius-lg); /* Rounded corners instead of circle */
    box-shadow: var(--shadow-md);
    position: relative;
    overflow: hidden;
  }

  .blog-info h3 {
    font-family: var(--font-serif);
    font-size: 2rem;
    color: var(--color-brown);
    margin-bottom: 0.75rem;
    line-height: 1.1;
  }

  .blog-info p {
    font-size: 1.05rem;
    line-height: 1.6;
    margin-bottom: 1.25rem;
    max-width: 600px;
    color: var(--text-secondary);
  }

  .blog-meta {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 1rem;
  }

  .blog-date {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--color-warm-gray);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .blog-tag {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    color: var(--accent-primary);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    padding: 0.2rem 0.5rem;
    background: rgba(42, 157, 143, 0.06);
    border-radius: var(--radius-sm);
  }

  .blog-link {
    font-family: var(--font-mono);
    font-size: 0.8rem;
    color: var(--accent-primary);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    font-weight: 600;
  }

  @media (max-width: 768px) {
    .blog-item {
      grid-template-columns: 100px 1fr; /* Keep side-by-side but smaller image */
      gap: 1.5rem;
    }
    .blog-item:hover {
      transform: translateY(-4px);
    }
    .blog-visual {
      aspect-ratio: 3/4; /* Keep it tall */
    }
  }

  @media (max-width: 640px) {
    .transistor-feature {
      height: 200px;
    }
    .feature-label-overlay h3 {
      font-size: 1.8rem;
    }
  }
</style>
